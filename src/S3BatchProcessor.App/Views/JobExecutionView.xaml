<UserControl x:Class="S3BatchProcessor.App.Views.JobExecutionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:S3BatchProcessor.App.ViewModels"
             xmlns:conv="clr-namespace:S3BatchProcessor.App.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:JobExecutionViewModel}"
             d:DesignHeight="300" d:DesignWidth="1000">
    <UserControl.Resources>
        <conv:JobStatusToColorConverter x:Key="StatusToColor" />
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <DockPanel Margin="6">
        <!-- Header bar with phase + progress + cancel -->
        <Border DockPanel.Dock="Top" Background="#F0F4FF" Padding="8" CornerRadius="3" Margin="0,0,0,4">
            <DockPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Execution" FontWeight="Bold" FontSize="14"
                               VerticalAlignment="Center" />
                    <Border Background="#E3F2FD" CornerRadius="3" Padding="6,2" Margin="8,0,0,0"
                            VerticalAlignment="Center">
                        <TextBlock Text="{Binding Phase}" FontSize="11" FontWeight="SemiBold"
                                   Foreground="#1565C0" />
                    </Border>
                    <TextBlock Text="{Binding ProgressSummary}" Margin="12,0,0,0"
                               VerticalAlignment="Center" FontSize="12" Foreground="#555" />
                </StackPanel>
                <Button DockPanel.Dock="Right" Content="Cancel All"
                        Command="{Binding CancelJobsCommand}"
                        Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}"
                        Padding="12,4" Foreground="Red" FontWeight="SemiBold"
                        HorizontalAlignment="Right" />
            </DockPanel>
        </Border>

        <!-- Pre-flight panel (visible during instance startup) -->
        <Border DockPanel.Dock="Top" Background="#FFFDE7" BorderBrush="#FDD835" BorderThickness="1"
                CornerRadius="3" Padding="10,8" Margin="0,0,0,4"
                Visibility="{Binding IsPreFlightActive, Converter={StaticResource BoolToVis}}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="{Binding PreFlightStatus}"
                           FontWeight="SemiBold" FontSize="12" Foreground="#F57F17" Margin="0,0,0,4" />
                <ItemsControl ItemsSource="{Binding PreFlightLog}" MaxHeight="120">
                    <ItemsControl.Template>
                        <ControlTemplate TargetType="ItemsControl">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </ControlTemplate>
                    </ItemsControl.Template>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontSize="11" Foreground="#555"
                                       Margin="8,1,0,1" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DockPanel>
        </Border>

        <!-- No results placeholder -->
        <TextBlock Text="No execution results yet. Assign files and click Run Processing above."
                   Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="13">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Results.Count}" Value="0" />
                                <Condition Binding="{Binding IsPreFlightActive}" Value="False" />
                                <Condition Binding="{Binding IsRunning}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible" />
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Results list -->
        <DataGrid ItemsSource="{Binding Results}" AutoGenerateColumns="False"
                  IsReadOnly="True" BorderThickness="1"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#E0E0E0"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.CanContentScroll="False"
                  CanUserResizeRows="True"
                  SelectionMode="Single"
                  MouseLeftButtonUp="DataGrid_MouseLeftButtonUp">
            <DataGrid.Resources>
                <Style x:Key="CopyableText" TargetType="TextBox">
                    <Setter Property="IsReadOnly" Value="True" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="TextWrapping" Value="Wrap" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="FontSize" Value="12" />
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Instance" Width="110"
                                    Binding="{Binding Instance.NameTag}" />
                <DataGridTextColumn Header="File" Width="140"
                                    Binding="{Binding File.Name}" />
                <DataGridTemplateColumn Header="Status" Width="75">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Status}" FontWeight="SemiBold"
                                       Foreground="{Binding Status, Converter={StaticResource StatusToColor}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Duration" Width="65"
                                    Binding="{Binding Duration}" />
                <DataGridTemplateColumn Header="Output" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ScrollViewer MaxHeight="100" VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Disabled">
                                <TextBox Text="{Binding Output, Mode=OneWay}"
                                         Style="{StaticResource CopyableText}"
                                         Foreground="Gray" />
                            </ScrollViewer>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Stderr" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ScrollViewer MaxHeight="100" VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Disabled">
                                <TextBox Text="{Binding ErrorOutput, Mode=OneWay}"
                                         Style="{StaticResource CopyableText}"
                                         Foreground="#D32F2F" />
                            </ScrollViewer>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
